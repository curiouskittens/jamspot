{
  "name": "jamspot",
  "version": "0.1.0",
  "description": "Find and connect with fellow musicians to set up a jam session, collaborate and create the next big hit.",
  "private": true,
  "proxy": "http://localhost:3001/",
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-router-dom": "^4.3.1",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
